# Payload CMS
PAYLOAD_SECRET=command-center-dev-secret-change-me

# Seed passwords (used during first-run user creation)
SEED_PASSWORD=barducks

# E2E test user credentials (test@cc.local)
E2E_TEST_PASSWORD=barducks

# Service ports
NEXT_PUBLIC_OPENCODE_PROXY_PORT=4097
NEXT_PUBLIC_TERMINAL_WS_PORT=3223
NEXT_PUBLIC_STORYBOOK_PORT=6006
OPENCODE_PORT=4096
