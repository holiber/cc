import React from 'react'

/**
 * Custom icon shown in the Payload admin nav bar.
 * Replaces the default Payload icon.
 */
export default function Icon() {
    return (
        <span style={{
            fontSize: '18px',
            fontWeight: 700,
            color: '#fff',
        }}>
            ⌘
        </span>
    )
}
